from setuptools import find_packages, setup

setup(
    name="smalivm",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.6",
    install_requires=[],
    include_package_data=True,
    entry_points={},
)
